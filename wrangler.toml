# Cloudflare Pages configuration
name = "raiou"
type = "javascript"
account_id = ""
workers_dev = true
route = ""
zone_id = ""

# Environment variables
[env.production]
vars = { ENVIRONMENT = "production" }

[env.preview]
vars = { ENVIRONMENT = "preview", STORYBLOK_IS_PREVIEW = "yes" }

# Build settings - handled by Cloudflare Pages directly
# Framework: Astro
# Build command: npm run build
# Build output directory: dist
